# KurenaiEngine — Codex 向けの入口

このファイルは OpenAI Codex（および AGENTS.md を読むエージェント）向けの入口である。
Claude Code は [CLAUDE.md](CLAUDE.md) を読むので、**規約の本体はそちらにあり、ここには
複製しない**。二重管理にすると必ず片方が古くなる。

## 最初にやること

**[CLAUDE.md](CLAUDE.md) を読むこと。** 描画上の前提・ビルド手順・アセットの形式・
ドキュメントの書き分けは、すべてそこに書いてある。ここに書いていないから存在しない、
のではない。

横断規約（PID の扱い・GUI 起動禁止・無断コミット禁止・文字コード）は `~/.codex/AGENTS.md`
にある。Codex はこのファイルと合わせて自動で読むので、探しに行かなくてよい。

## 特に取り違えやすい3点（CLAUDE.md の再掲）

読まずに手を動かすと静かに間違ったまま進むものだけ、ここに再掲する。

1. **深度バッファは Reverse-Z。** 近平面が NDC z=1.0、遠平面が z=0.0。
   「深度値が小さいほど遠い」「背景（ジオメトリ無し）は深度 0」であり、
   最も手前を取るなら `max`、最も遠くを取るなら `min`。
   **逆に書いても絵は出る**ので気づけない。

2. **シェーダはビルド対象。** `.hlsl` を触ったらビルドが要る。`KurenaiShaderPacker.exe`
   がビルドイベントで走り、`.hlsl` を `.kshader`（事前コンパイル済みパッケージ）へ焼いて
   出力フォルダの `Shaders/` に置く。**実行時の HLSL コンパイルは無く、出力フォルダに
   `.hlsl` はコピーされない**ので、差し替えて起動し直すやり方は使えない。

3. **DX11 と DX12 の両対応。** `Source/Library/RHI/` の抽象化層で吸収している。
   **RHI のインターフェースを変えたら DX11 / DX12 の両方を直す。**
   片方だけ直すと、もう片方は起動して初めて壊れていることが分かる。

## このリポジトリでやらないこと

- **ビルドと起動はしない。** ビルド・実行・スクリーンショット・描画結果の良し悪しの
  判断は、あなたを呼び出した側（Claude Code またはユーザー）の担当である。
  確認が要るなら、何を確かめてほしいかを書いて返すこと。
- **コミットと push はしない。** 変更は作業ツリーに残すだけにする。
- **`Assets/` を再生成しない。** Git 管理外で 180MB 規模あり、再パックには時間がかかる。
  必要なら、なぜ再パックが要るかを報告して返すこと。
