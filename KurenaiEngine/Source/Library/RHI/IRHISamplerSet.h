#pragma once

#include "KurenaiTypes.h"

namespace Kurenai::RHI
{
    // 1つの描画パスがまとめてバインドするサンプラーの組(バンク)。
    //
    // サンプラーを1個ずつ差し替えるAPIにしていない理由:
    // DX12のサンプラーはシェーダ可視ディスクリプタヒープ経由でバインドされ、ヒープへの書き込みは
    // CPUタイムラインで即座に反映される。このエンジンは1フレーム分のコマンドをすべて記録してから
    // 1回だけExecuteCommandListsする設計(DX12Device.h参照)のため、パスごとに同じヒープスロットへ
    // 別のサンプラーを書くと、GPUが実際に描画する時点ではそのフレーム最後に書かれた1つに
    // すべて上書きされてしまう(パスの実行順を変えるだけで絵が変わる。しかもDX11は即時コンテキストへ
    // PSSetSamplersを発行するため正しく動いてしまい、DX11とDX12で絵が食い違う)。
    //
    // そこでセット単位で扱う。セットの中身はここで一度作ったら二度と書き換えず、パスごとに
    // 切り替えるのはディスクリプタテーブルの先頭位置だけにする。こうすると上書きが原理的に起きない。
    // この不変性が前提なので、IRHIDevice::CreateSamplerSetは描画開始前(初期化時)にのみ呼ぶこと。
    class KURENAI_LIB_API IRHISamplerSet
    {
    public:
        virtual ~IRHISamplerSet() = default;
    };
}
